node_modules/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/
android/*.iml
android/.idea/
*.hprof

# dados reais de teste (endereços/clientes) — nunca comitar
testdata/

# chave de admin do Firebase (Service Account) — essa sim é secreta de
# verdade, dá acesso total ao backend. NUNCA comitar.
# (google-services.json e .firebaserc NÃO são segredo — identificam o
# app/projeto, mas não dão acesso a nada sozinhos; por isso ficam
# versionados normalmente, senão o build quebra em qualquer clone novo)
backend/serviceAccountKey*.json
