node_modules/
android/local.properties
android/.gradle/
android/build/
android/app/build/
android/captures/
android/*.iml
android/.idea/
*.hprof

# dados reais de teste (endereços/clientes) — nunca comitar
testdata/

# credenciais do Firebase, uma vez que existirem — nunca comitar
# (o .firebaserc.example, sem segredo nenhum, continua versionado)
backend/.firebaserc
backend/serviceAccountKey*.json
android/app/google-services.json
**/google-services.json
